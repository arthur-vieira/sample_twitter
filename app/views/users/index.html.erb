<% provide(:title, "All users") %>
<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %> <%# automatically iterates, passing each user to the _user.html.erb partial %>
</ul>

<%= will_paginate %>
